<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ban.mapper.SysUserMapper">

    <select id="loadUserById" resultType="sys_user">
		select * from sys_user where id = #{id}
	</select>

</mapper>