package com.ban.obj;

// 继承
public class Extend {
    // 继承细节 执行顺序 静态块等  super（父类）
    // 父 静态变量 静态块  子 静态变量 静态块  父 变量 块 构造  子 变量 块 构造

    // 包概念 访问修饰符范围
    // private(本类) default(本包) protected(其它包继承类) public(所有包)

    // final 值类型(值不变) 引用类型(引用地址不变)
    // 类中的static被所有对象共享 初始化一次 （多线程安全问题）

    public static void main(String[] args) {

    }
}
