package com.ban.grammar;

// 运算
public class Calc {
    // byte short char 在算术运算中会自动转为int 运算
    // 算术运算 + - * / %

    // 赋值运算 = += -= *= /= %=

    // 自增 自减  ++ --

    // 关系运算  == != >= > <= <

    // 逻辑运算 &(都运算都为true则为true)  |(都运算都为false为false)  ^(两边不同则为true)  !(单值取反)

    // 短路运算 && ||

    // 三元运算符 ?:
    public static void main(String[] args) {

    }
}
